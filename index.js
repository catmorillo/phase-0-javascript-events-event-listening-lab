function addingEventListener(clickAlert) {

const input = document.getElementById('button');

function clickAlert(addingEventListener) {
  alert('I was clicked!');
}

input.addEventListener('click', clickAlert);
}